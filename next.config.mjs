/** @type {import('next').NextConfig} */
const nextConfig = {
    images: {
        domains: ['images.ecestaticos.com',"upload.wikimedia.org","avatars.githubusercontent.com"],
      },
};

export default nextConfig;
